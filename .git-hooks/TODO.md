# TODO

- git hooks dispatcher install
- git hooks dispatcher activate
- git hooks add <name>
- git hooks dispatcher deactivate
- git hooks dispatcher uninstall
- Default hooks
    - pre-commit
        - deny-commit <branchname>
        - travis-lint